# Winsights Price Tracker

Tracks product prices daily and stores historical data.

## Setup

```bash
python -m venv .venv
```

Activate:

Windows:

```bash
source .venv/Scripts/activate
```

Install:

```bash
pip install -r requirements.txt
```

Run:

```bash
python src/price_tracker/main.py
```